# RAG System v2 — Environment Configuration
# Copy this to .env and fill in your keys

# OpenRouter (required for LLM judge, generation, query expansion)
OPENROUTER_API_KEY=sk-or-v1-...

# Pinecone (optional — for production vector store)
PINECONE_API_KEY=
PINECONE_INDEX_NAME=rag-alex
PINECONE_CLOUD=aws
PINECONE_REGION=us-east-1

# ChromaDB (local fallback — no key needed)
CHROMADB_PERSIST_DIR=./chroma_db

# Ollama (local fallback — must be running)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:4b
